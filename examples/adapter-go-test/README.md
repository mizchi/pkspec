# adapter-go-test

Pkl-only `go test` adapter configuration. It models `go test` as a
generic adapter instead of a hard-coded Go-side runner.

Validate the DSL shape:

```sh
pkl eval examples/adapter-go-test/Adapter.pkl
```
