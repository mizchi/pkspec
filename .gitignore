/bin/
/pkt
/result
/result-*
*.test
*.out

# Per-suite timings history (Phase 30). Snapshots under .pkthunder/snapshots/
# ARE meant to be committed, so don't gitignore .pkthunder/ wholesale.
.pkthunder/timings.jsonl

# experiments produce snapshot files we DO want to commit
!experiments/**/*.pkl-expected.pcf
